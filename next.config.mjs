/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/ydslien',
  assetPrefix: '/ydslien',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
